# Synchri v0.5.3 — Reliable CLI discovery from the macOS app

This patch release restores automatic launch of connected Claude Code,
Codex, and GitHub Copilot CLIs when Synchri is opened from Finder or
LaunchServices. Connected runtimes now resolve through deterministic,
runtime-scoped desktop discovery instead of falling back to pasted prompts
because macOS started the app with a minimal system `PATH`.

**Status: implemented and verified.**

## Baseline

- **Previous version: v0.5.2**, released from `codex/release-v0.5.2` and
  merged to `main` at `3add12e`.
- This patch ships as **v0.5.3** from branch
  `codex/fix-desktop-cli-discovery`.

## Scope

- [x] The native macOS entry point restores validated connected executables
  into a provider-scoped resolver and prepares a deterministic desktop `PATH`
  from standard per-user tool directories, common version-manager shims,
  Homebrew, `/usr/local/bin`, and the Codex binary bundled with ChatGPT.
- [x] Connected runtime records are read defensively and read-only; failed,
  unknown, mismatched, or non-executable records cannot alter discovery.
- [x] Managed launches, planning launches, connection tests, resume canaries,
  and recovery resumes pin the resolved absolute executable while preserving
  prompt and resume placeholders. Each maintained child puts that executable's
  directory first in its own `PATH`, keeping Node-based launchers paired with
  the nvm/fnm installation that owns them.
- [x] Shell startup files are never sourced, keeping desktop launch
  deterministic and avoiding arbitrary shell initialization side effects.
- [x] The frozen-sidecar build now runs a Finder-equivalent smoke test with a
  temporary connected Claude CLI and refuses to package an app that would
  regress to manual prompts.

## Verification record

- Full Python suite: **748 passed** (8 focused macOS entry-point and runtime
  resolution tests added in this patch).
- Focused macOS entry-point and desktop-release suite: **12 passed**.
- The frozen Apple Silicon sidecar reports connected Claude Code, Codex, and
  GitHub Copilot runtimes ready under Finder's minimal `PATH`.
- The native Tauri application builds successfully for Apple Silicon, and all
  four Rust tests pass.
